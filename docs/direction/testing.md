# Automated tests

No test runner is configured (`package.json` has no test script). Not necessarily wrong for a solo project at this stage, but worth an explicit decision rather than silent omission as the app grows.
