// The song list is rendered directly in (browse)/layout.tsx, not through
// this slot — see the comment there. This page only exists to make "/" a
// valid match for the unnamed slot.
export default function BrowsePage() {
  return null;
}
