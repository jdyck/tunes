// The songs list is rendered by the shared browse layout. This route gives
// the list its own mobile URL while desktop keeps the same multi-pane view.
export default function SongsPage() {
  return null;
}
