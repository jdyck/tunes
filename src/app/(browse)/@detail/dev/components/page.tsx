// DetailPaneGate shows its own "Pick a component" placeholder instead of
// this content for the bare /dev/components path -- this page only exists
// so that URL resolves to something (see DetailPaneGate for why).
export default function DevComponentsIndexPage() {
  return null;
}
